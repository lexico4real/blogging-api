export enum Role {
  User = 'User',
  Admin = 'Admin',
  Super_Admin = 'Super_Admin',
}

// For CASL
export enum Permissions {
  Manage = 'manage',
  Create = 'create',
  Read = 'read',
  Update = 'update',
  Delete = 'delete',
}
